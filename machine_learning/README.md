# Machine Learning

Classical machine-learning pipelines for EEG PSD-based classification.

Files:
- `binary_classification.py`: CLI entrypoint for subject-level grouped CV and model evaluation.
- `requirements.txt`: Pinned Python dependencies.
- `classification_results.txt`: Appended results log (created when first run).

Usage:
```bash
python binary_classification.py --help
```
